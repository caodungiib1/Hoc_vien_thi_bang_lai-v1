# Checklist Phase 19: Kết nối màn hình với lớp services

- [x] Lập kế hoạch triển khai Phase 19.
- [x] Chờ người dùng phê duyệt kế hoạch.
- [x] Rà soát các màn hình còn import trực tiếp mock data.
- [x] Chuyển `Dashboard.jsx` sang lấy dữ liệu học viên qua `studentService`.
- [x] Chuyển `Students.jsx` sang lấy danh sách học viên qua `studentService`.
- [x] Chuyển `StudentDetail.jsx` sang lấy chi tiết học viên qua `studentService`.
- [x] Chuyển `Exams.jsx` sang lấy dữ liệu lịch thi qua `examService`.
- [x] Chuyển `Fees.jsx` sang lấy dữ liệu học phí qua `feeService` và danh sách học viên qua `studentService`.
- [x] Chuyển `Notifications.jsx` sang lấy danh sách học viên qua `studentService`.
- [x] Kiểm tra build và phản hồi các route chính.
- [x] Đánh dấu hoàn tất Phase 19.
